# Projeto 3 — API (Newman) – CI

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - pipeline-api/**
      - api-auth-automation/**

pool:
  vmImage: 'windows-latest'

steps:
- checkout: self
  fetchDepth: 1

- task: NodeTool@0
  inputs:
    versionSpec: '20.x'
  displayName: 'Instalar Node.js 20'

# (Item 1) Instalar dependências do projeto (auto-contido)
- powershell: |
    cd "$(Build.SourcesDirectory)/api-auth-automation"
    npm ci
    npx newman -v
  displayName: 'Instalar dependências (npm ci) - API'

# (Item 2) Executar Newman (Contrato) via npx (blindado contra aspas/path)
- powershell: |
    $changed = git diff --name-only HEAD~1 HEAD | Select-String "^api-auth-automation/"

    if (-not $changed) {
      Write-Host "Nenhuma mudança em api-auth-automation. Pulando Contract Gate."
      exit 0
    }

    $base    = Join-Path "$(Build.SourcesDirectory)" "api-auth-automation"
    $reports = Join-Path $base "reports"

    New-Item -ItemType Directory -Force -Path $reports | Out-Null

    node (Join-Path $base "tools\\sync-schemas-to-env.js")

    $ts    = Get-Date -Format "yyyy-MM-dd_HHmmss"
    $html  = Join-Path $reports ("Resultado_API_Contrato_{0}.html" -f $ts)
    $junit = Join-Path $reports "junit.xml"

    $collection = Join-Path $base "Auth.postman_collection.json"
    $envFile    = Join-Path $base "Auth_DummyJSON.postman_environment.json"

    npx newman run $collection `
      -e $envFile `
      -r "cli,htmlextra,junitfull" `
      --reporter-htmlextra-export $html `
      --reporter-junitfull-export $junit

    Write-Host "=== Arquivos gerados em reports ==="
    Get-ChildItem -Path $reports -Force | Format-Table Name, Length
    Write-Host "=== Conteúdo da pasta reports ==="
    dir .\reports

    if (-not (Get-ChildItem -Path $reports -File -ErrorAction SilentlyContinue)) {
      Write-Error "Nenhum arquivo foi gerado em reports/. Verifique comando do Newman/reporters."
      exit 1
    }
  displayName: 'Executar Newman (Contrato) - npx'

- powershell: |
    $p = "$(Build.SourcesDirectory)/api-auth-automation/reports"
    if (Test-Path $p) {
      Write-Host "✅ reports existe: $p"
      Write-Host "##vso[task.setvariable variable=hasReports]true"
      Get-ChildItem $p -Force | Format-Table Name, Length
    } else {
      Write-Host "⚠️ reports NÃO existe: $p"
      Write-Host "##vso[task.setvariable variable=hasReports]false"
    }
  displayName: "Checar pasta reports"
  condition: succeededOrFailed()

- powershell: |
    $p = "$(Build.SourcesDirectory)/api-auth-automation/reports"
    if (Test-Path $p) {
      Write-Host "✅ reports existe: $p"
      Write-Host "##vso[task.setvariable variable=hasReports]true"
      Get-ChildItem $p -Force | Format-Table Name, Length
    } else {
      Write-Host "⚠️ reports NÃO existe: $p"
      Write-Host "##vso[task.setvariable variable=hasReports]false"
    }
  displayName: "Checar pasta reports"
  condition: succeededOrFailed()

# Publicar Artifact (pasta reports)
- task: PublishPipelineArtifact@1
  condition: and(succeededOrFailed(), eq(variables.hasReports, 'true'))
  inputs:
    targetPath: '$(Build.SourcesDirectory)/api-auth-automation/reports'
    artifact: 'contract-report'
    publishLocation: 'pipeline'
  displayName: "Publicar relatório de contrato (HTML)"
  
# Publicar JUnit (aparece na aba Tests)
- task: PublishTestResults@2
  condition: succeededOrFailed()
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: 'junit.xml'
    searchFolder: '$(Build.SourcesDirectory)/api-auth-automation/reports'
    failTaskOnFailedTests: false
  displayName: 'Publicar JUnit (Newman)'